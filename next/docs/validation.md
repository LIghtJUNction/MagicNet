# 验证证据与边界

## 当前宿主机与 CI 验证

2026-09-26：本地 Rust/Cargo 1.98.1、Node 22.16.0、Python 3.13、系统 Chromium；GitHub 验证使用 Rust 1.98.1、Node 24。订阅源码验证运行 `36243944429` 成功，保存了逐文件 SHA-256、已验证源码和浏览器结果。

实际通过：kamfw 12 项，业务引擎/订阅 38 项，CLI/worker/安装/打包 39 项，前端协议 8 项，浏览器集成 12 项。另执行了 Cargo fmt、Clippy（警告视为错误）、Vue 类型检查和生产构建。计数是这次执行结果，不是永久常量。

浏览器测试覆盖四页和 320/375/768/1440 像素，测试专用 KernelSU SDK 适配器把请求交给真实编译的宿主机 CLI。包含订阅替换、全删、本地导入、Base64、部分拒绝计数、错误及草稿保留。界面截图已在本地查看。它不等于 Android WebView/CSP、设备内核路由或真实应用联网验收。

事务测试在 Store 修改前后 48 个位置中断并恢复。进程级测试包括受控父进程、启动门、三秒优雅清理、重复停止、失败恢复、噪声日志限制及监督器崩溃。它们不模拟全部磁盘断电语义或全部 Android 内核行为。native readiness 仍只观察存活，诊断不将其写为网络健康。

## 旧生产回归修复

提交 `056d99375c25fcdb70aa4001e0496e960d068983` 修正了两类测试夹具问题，没有降低生产检查：

- fake curl 现在返回真实调用要求的 HTTP 状态、重定向元数据和响应头；安装缓存写入测试私有目录。完整 fake-Magisk smoke 已在本地通过。
- Android 测试 ZIP 仅在 `cli` 与原 `bin/magicnet-cli` 字节一致时一起替换成 x86_64；安全符号链接保留，其他二进制仍拒绝。复制 ZipInfo 后写入，避免修改输入 ZIP 的偏移。34 项归档/模拟器契约测试通过。

此前 `cca36323fa98e0a9e83559de2729301a81febe5f` 的 Android 运行 `36235025570` 在准备阶段因未替换的 `cli` ELF 失败，不能据此声称设备生命周期通过。后续设备结果须读取对应提交的独立 Android 运行，不能用上面通过的宿主机检查替代。

## 仍待完成

生产替换要求旧版迁移、Tailscale、完整网络能力、机器接口兼容及真实 Android 生命周期回归。`cutover.json` 继续明确标注尚未接受的项目；独立源码提交并不意味着切换生产入口。物理设备/OEM/真实应用结果尚未获取。

MagicNet 原始代码保存在 `archive/pre-rewrite-20260926`。kamfw 的独立、字节一致归档另见 `kamfw-archive.json`，不再仅依赖上游提交是否一直可访问。
